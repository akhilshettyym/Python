# 4. Replace the double space from problem 3 with single spaces.

name = " You  have a  really  great  project "

print(name)
print(name.replace("  "," "))


# Strings are immutable which means we cannot change them by running any functions on them.